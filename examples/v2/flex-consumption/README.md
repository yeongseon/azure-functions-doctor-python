# Flex Consumption reference example

Healthy Flex Consumption app: `functionAppConfig.runtime` python 3.12,
deployment storage with a container URL and system-assigned managed identity,
no legacy app settings. Run:

    azure-functions-doctor doctor --path examples/v2/flex-consumption --profile deploy
